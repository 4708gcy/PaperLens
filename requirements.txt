# ════════════════════════════════════════════════════════════
# PaperLens 依赖清单（基于 conda ocr 环境实测版本，2026-06）
# ════════════════════════════════════════════════════════════
#
# ⚠️ 安装顺序很重要！PyTorch 必须单独装（用 CUDA 专用源），
# 否则 pip 会装成 CPU 版导致 GPU 无法用。
#
# 步骤 1：先装 PyTorch（CUDA 12.1 版）
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#
# 步骤 2：再装其余依赖
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#


# ── PyTorch（⚠️ 请用上面的单独命令安装，不要用 pip install -r 装）──
# torch==2.5.1+cu121
# torchvision==0.20.1+cu121
# torchaudio==2.5.1+cu121


# ── Web 框架 ──
fastapi==0.136.3
uvicorn==0.49.0
starlette==1.3.1
python-multipart==0.0.32


# ── 数据校验与配置 ──
pydantic==2.13.4
pydantic_core==2.46.4
PyYAML==6.0.3
python-dotenv==1.2.2


# ── LLM 调用 ──
openai==2.41.1


# ── Agent 编排（LangChain + LangGraph）──
langchain==1.3.9
langchain-core==1.4.7
langchain-openai==1.3.2
langgraph==1.2.5
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langchain-protocol==0.0.17
langsmith==0.8.15


# ── Embedding 与检索 ──
sentence-transformers==5.5.1
elasticsearch==9.4.1
elastic-transport==9.4.1


# ── 重排序（transformers，与 Embedding 共用 torch）──
transformers==4.55.0
accelerate==1.13.0
safetensors==0.8.0
huggingface_hub==0.36.2
tokenizers==0.21.4
tiktoken==0.13.0
peft==0.15.0


# ── 文档解析 ──
pypdf==6.12.2


# ── 数据库 ──
SQLAlchemy==2.0.50
aiosqlite==0.22.1


# ── 前端（Streamlit）──
streamlit==1.58.0
httpx==0.28.1
altair==6.2.1
pydeck==0.9.2
watchdog==6.0.0


# ── 数据处理 ──
numpy==1.26.4
pandas==2.2.2
pyarrow==24.0.0
scikit-learn==1.5.1
scipy==1.17.1


# ── 测试 ──
pytest==9.1.0
pytest-asyncio==1.4.0


# ── 评测可视化 ──
matplotlib==3.9.2


# ── 其他间接依赖（锁定版本避免冲突）──
Jinja2==3.1.6
orjson==3.11.9
tenacity==9.1.4
rich==15.0.0
requests==2.34.2
tqdm==4.68.2
filelock==3.29.2
regex==2026.5.9

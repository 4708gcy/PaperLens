# ── Web 框架 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── 数据校验与配置 ──
pydantic>=2.10.0
pyyaml>=6.0
python-dotenv>=1.0.0

# ── LLM 调用 ──
openai>=1.60.0

# ── Agent 编排 ──
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0

# ── Embedding 与检索 ──
sentence-transformers>=3.0.0
elasticsearch>=8.15.0

# ── 重排序（与 Embedding 共用 transformers/torch） ──
transformers>=4.46.0
torch>=2.5.0

# ── 文档解析 ──
pypdf>=5.0.0

# ── 数据库 ──
sqlalchemy>=2.0.0

# ── 前端 ──
streamlit>=1.40.0
httpx>=0.27.0

# ── 数据处理 ──
numpy>=1.26.0
pandas>=2.2.0

# ── 测试 ──
pytest>=8.0.0
pytest-asyncio>=0.24.0

# ── 评测可视化 ──
matplotlib>=3.8.0
